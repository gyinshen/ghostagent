

export const ReportsListItemNoAuth = (): JSX.Element => {
  // const {
  //   selected,
  // } = useReportsListItemNoAuth(report);

  return (
    <div>

      <div className="flex items-center gap-2">
        

      </div>
      

      {/* Fade to white */}
      <div
        aria-hidden
        className="not-sr-only absolute left-1/2 top-0 bottom-0 right-0 bg-gradient-to-r from-transparent to-white dark:to-black pointer-events-none"
      >
        NO REPORT
      </div>
    </div>
  );
};
